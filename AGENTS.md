# 项目协作指南

## 项目概览

- 这是一个“大众点评”风格的中文单页首页，使用 Vue 3、Vite 和 TypeScript。
- 项目只包含前端展示与本地交互；没有 Vue Router、后端接口、登录、数据库或持久化状态。
- 默认城市为上海；页面提供城市选择、搜索提示、分类入口、商家收藏和榜单/社区展示。

## 技术栈与运行

- Node.js：`>=22.13.0`
- 包管理器：pnpm；提交并维护 `pnpm-lock.yaml`，不要混用 npm 生成 `package-lock.json`。
- 本地开发：`pnpm dev`，默认地址为 `http://127.0.0.1:5173`。
- 生产构建：`pnpm build`，该命令会先运行 `vue-tsc -b`，再执行 Vite 构建。
- 本地预览构建产物：`pnpm preview`。

## 代码结构

- `src/main.ts`：Vue 应用入口，挂载 `App.vue` 并导入全局样式。
- `src/App.vue`：唯一的页面组件；页面数据、`ref` 状态、事件处理、模板及城市选择器局部样式都在此文件。
- `app/globals.css`：全站视觉样式和响应式规则。目前为压缩格式；修改时保持既有橙红、米白视觉基调和移动端适配。
- `index.html`：中文页面语言、标题、描述及 Open Graph 元信息。
- `public/`：静态资源；`public/og.png` 是社交分享图，保留其引用路径。
- `vite.config.ts`：Vue Vite 插件配置，开发服务器绑定 `127.0.0.1`。

## 开发约定

- 使用 Vue Composition API 与 `<script setup lang="ts">`；不要引入 React、Next.js 或服务端框架。
- 展示型数据（分类、商家、城市分组、榜单）目前定义在 `src/App.vue` 顶部。小范围内容调整优先直接维护这些常量。
- 当前交互只更新内存状态和页面提示；除非需求明确，不要增加接口、路由、localStorage 或全局状态库。
- 新增按钮应提供清晰的可访问名称；保留键盘 Enter 触发搜索的行为。
- 城市列表按拼音首字母分组排列。新增或调整城市时，保持字母组及组内城市的排序。
- 维持当前桌面三列商家卡片、移动端单列卡片与分类四列的响应式布局。

## 验证与版本控制

- 每次修改页面或配置后运行 `pnpm build`。
- 手动检查首页加载、城市切换、搜索提示、分类点击、收藏状态和移动端布局。
- 不提交 `node_modules/`、`dist/`、`.env*`、`.pnpm-store/`、Vite/TypeScript 缓存或 IDE 配置；现有 `.gitignore` 已覆盖这些路径。
- GitHub 远程为 `origin`，使用 SSH。推送前确认工作区状态与分支历史，涉及回滚或强制推送时必须先明确说明影响。
