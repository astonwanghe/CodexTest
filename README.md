# 大众点评首页（Vue 3）

基于 Vue 3、Vite 和 TypeScript 构建的单页点评网站首页。

## 本地运行

```bash
pnpm install
pnpm dev
```

默认访问地址为 `http://127.0.0.1:5173`。

## 构建

```bash
pnpm build
```

页面包含搜索提示、分类入口、商家收藏、榜单和社区内容等纯前端交互；不依赖路由、后端接口或数据持久化服务。
